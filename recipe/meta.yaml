{% set name = "sphinxawesome-theme" %}
{% set version = "6.0.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/sphinxawesome_theme-{{ version }}.tar.gz
  sha256: 8c6ccb6c1bcfce2e3438e872274075d6f520843faa6f7fd0cb0972c39cb279ba

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - poetry
    - pip
  run:
    - python >={{ python_min }}
    - sphinx >=7.2.0,<7.3
    - beautifulsoup4 >=4.9.1,<5.0.0

test:
  imports:
    - sphinxawesome_theme
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://sphinxawesome.xyz
  summary: An awesome theme for the Sphinx documentation generator
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - apmorton
